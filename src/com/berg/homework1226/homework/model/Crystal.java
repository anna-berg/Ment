package com.berg.homework1226.homework.model;

import java.util.Arrays;
import java.util.List;

public enum Crystal {
    RED,
    WHITE;

    public static final List<Crystal> CASHED_CRYSTALS = Arrays.asList(values());
}
